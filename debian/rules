#!/usr/bin/make -f
# -*- makefile -*-

export DH_VIRTUALENV_INSTALL_ROOT=/opt
export PYBUILD_NAME=wlanpi-core

PACKAGE=$(shell dh_listpackages)
VERSION=$(shell parsechangelog | grep ^Version: | sed -re 's/[^0-9]+([^-]+).*/\1/')

SNAKE=/usr/bin/python3

DH_VENV_DIR=debian/$(PACKAGE)$(DH_VIRTUALENV_INSTALL_ROOT)/$(PACKAGE)

DBUS_PYTHON_SPEC=$(shell grep dbus-python== endpoint_requires.txt | cut -f1 -d ",")
PKG_PREFIX=debian/wlanpi-core

%:
	dh $@ --with python3,python-virtualenv --buildsystem=pybuild

.PHONY: override_dh_virtualenv override_dh_installexamples 

# we don't really want to strip the symbols from our object files.
override_dh_strip:

override_dh_installexamples:
	dh_installexamples -A -X__pycache__

override_dh_virtualenv:
	dh_virtualenv \
		--install-suffix "wlanpi-core" \
		--builtin-venv \
		--python ${SNAKE} \
		--upgrade-pip-to=22.3 \
		--preinstall="setuptools==65.5.0" \
		--preinstall="wheel==0.37.1"
		--preinstall="mock" \
		--extra-pip-arg="--no-cache-dir" \
		--extra-pip-arg="--compile" \
		--extras="all,systemd,test"

	$(DH_VENV_DIR)/bin/python $(DH_VENV_DIR)/bin/pip install -I --no-binary dbus-python $(DBUS_PYTHON_SPEC)

