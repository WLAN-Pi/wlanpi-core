#!/bin/bash
# see: dh_installdeb(1)
# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

set -e

if dpkg -s nginx-full >/dev/null 2>&1; then
    # Remove nginx-full if it's installed
    dpkg -r --force-depends nginx-full
fi

#DEBHELPER#
